const db = require('../models/index');
const uuid = require('uuid/v4');

module.exports = {
  create(req, res) {},

  getAll(req, res) {},

  getById(req, res) {},

  update(req, res) {},

  delete(req, res) {},
};
